"""
The ai half of the pact contract: the typed result shapes and the helper
that builds one, kept dependency-free so a sandbox can assemble and encode
a ToolResult on its own, away from the rest of tokeo.
"""
